# secrets & platform linking — never commit
.env
.env.*
.vercel/

# dependencies
node_modules/

# large generated / downloaded assets (regenerated by tools/localize-assets.js,
# audio lives in Vercel Blob) — kept out of git to keep the repo light
s/
scores/
tools/audio-cache/
prev/
*.pdf

.DS_Store

# Codex delegation / instruction briefs — internal only, must never be pushed
codex-*.md
composerdle-add-pieces.md
